{
  "name": "create-react-native-app",
  "version": "0.1.0",
  "private": false,
  "description": "Create a React Native app from a bundled starter template.",
  "bin": {
    "create-react-native-app": "./bin/create-react-native-app.js"
  },
  "files": [
    "bin",
    "dist",
    "template",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepack": "npm run build",
    "test": "npm run build && node --test tests/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
